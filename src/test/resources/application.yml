spring:
  application:
    name: sproutfx-oauth-authorization
  profiles:
    active: test
  cloud:
    config:
      enabled: false
#  config:
#    import:
#      - configserver:http://config.sproutfx.kr